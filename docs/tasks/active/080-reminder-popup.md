# 080 애플리케이션 자체 알림 팝업

## 상태
- 상태: 활성
- 브랜치: `feat/reminder-popup`

## 목표
Windows 알림센터를 사용하지 않고 일정 시각에 제목과 메모를 보여주는 앱 자체
알림 팝업과 안정적인 예약 엔진을 제공한다.

## 의존성
- 작업 030, 040, 050, 070 완료

## 범위
- 가짜 시계로 테스트 가능한 `ReminderScheduler`를 구현한다.
- 정시, 5·15·30분, 1시간, 1일 전과 사용자 지정 오프셋을 지원한다.
- 시간이 없는 일정은 별도 알림 시각을 필수로 입력한다.
- 팝업에 확인, 5·10·30분 미루기와 편집 열기를 제공한다.
- 시작 시 최근 24시간 내 놓친 알림만 한 번 복구한다.

## 제외 범위
- Windows 알림센터, 서버 푸시와 앱 종료 중 실시간 팝업은 지원하지 않는다.
- 확인·미루기 상태를 계정 간 동기화하지 않는다.

## 설계 결정
- 알림 설정은 동기화하고 표시·확인·미루기 기록은 장치별 로컬 데이터로 둔다.
- 여러 알림이 동시에 도래하면 중복 창을 만들지 않고 순서대로 표시한다.
- 일정 시간은 실행 장치의 로컬 시간으로 해석한다.
- 시간 있는 일정은 발생 날짜와 시작 시각을, 시간 없는 일정은 사용자가 지정한
  발생 날짜의 알림 시각을 기준으로 분 단위 오프셋을 뺀 시점에 알린다.
- 편집기는 일정마다 알림 하나를 제공하고 정시·5·15·30분·1시간·1일 전 또는
  사용자 지정 분 오프셋을 선택한다. 기존 여러 알림 데이터는 저장 전까지 보존한다.
- 기간 일정은 시작일에 한 번만, 반복 일정과 기념일은 각 발생 날짜마다 예약한다.
- 예약 키는 계정 범위, 일정 ID, 발생 날짜, 오프셋과 별도 시각으로 구성하고 표시
  전에 장치별 기록을 저장해 재조회·재시작 중복을 막는다.
- 시작 시 원래 시각 또는 미루기 시각이 최근 24시간 안에 있는 항목만 한 번
  복구하며 그보다 오래된 항목은 표시하지 않는다.
- 앱 수명주기에서 예약 루프를 명시적으로 시작·취소·대기하고, 팝업 큐는 한 창만
  표시한다.

## 완료 조건
- 각 오프셋, 반복 발생 일정과 누락 복구에서 중복 없이 팝업이 열린다.
- 팝업에 메모가 표시되고 미루기와 편집 열기가 동작한다.

## 검증
- 정시·오프셋·무시간·누락·중복·미루기·종료 테스트를 가짜 시계로 추가한다.
- 앱 종료 정리와 여러 알림 큐를 수동 확인한다.
- `dotnet build tests/HmDesktopCalendar.RegressionTests/HmDesktopCalendar.RegressionTests.csproj --no-restore`
  성공, 경고 0개·오류 0개
- `dotnet run --project tests/HmDesktopCalendar.RegressionTests/HmDesktopCalendar.RegressionTests.csproj --no-build`
  회귀 테스트 46개 통과
- `server`에서 `pnpm test` 실행, 서버 테스트 9개 통과
- Avalonia 헤드리스 Skia 렌더링으로 640×560 최소 편집 창, 알림 입력 확장 상태와
  420×300 알림 팝업의 잘림·겹침·제목·메모 표시를 확인했다.
- 스크린샷: [최소 편집 창](../../screenshots/080/080-editor-minimum.png),
  [알림 입력](../../screenshots/080/080-editor-reminder.png),
  [알림 팝업](../../screenshots/080/080-reminder-popup.png)

## 작업 결과
- 커밋: 미정
- PR: 미정
