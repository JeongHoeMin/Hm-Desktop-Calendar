# 190 로컬 데이터 자동 백업

## 상태

- 상태: 활성
- 브랜치: `feat/local-backup`

## 목표

`%LocalAppData%\HmDesktopCalendar`의 일정 데이터를 자동 백업해 파일 손상과
실수 삭제로부터 복구 수단을 확보한다. 서버를 쓰지 않는 로컬 전용 사용자에게
특히 중요하다.

## 의존성

- 150 (설정 창의 "백업 폴더 열기" 버튼 배치만 해당. 백업 로직 자체는 독립)

## 범위

- `Services/BackupService`(신규): 앱 시작 후 백그라운드에서 데이터 파일
  (로컬 저장소 json과 `accounts\` 계정 스코프 디렉터리, settings.json 제외)을
  `backups\yyyyMMdd-HHmmss\`로 복사한다.
- 하루 1회 제한, 세대 10개 보관(초과분은 오래된 것부터 삭제).
- 설정 창에 "백업 폴더 열기" 버튼과 마지막 백업 시각 표시를 추가한다.
- 수동 복원 절차(앱 종료 → 백업 폴더의 파일을 원위치로 복사 → 재시작)를
  문서로 기록한다.

## 제외 범위

- UI 기반 자동 복원(파일 교체 후 재시작이 필요 — 문서화된 수동 절차로
  대체).
- 클라우드 백업.
- 서버 DB 백업(작업 220의 배포 문서에서 `pg_dump` 지침으로 다룸).

## 설계 결정

- 임시 폴더에 복사를 마친 뒤 최종 이름으로 이동해 부분 백업 폴더가 남지
  않게 한다.
- 백업 실패는 로그만 남기고 앱 동작에 영향을 주지 않는다.
- 저장소 파일이 쓰기 중일 수 있으므로 공유 읽기 모드로 복사하고, 실패한
  파일은 다음 주기에 재시도한다.
- 백업 루트 조회·생성 실패도 결과 오류로 반환하며 설정 창이나 앱 초기화를
  중단시키지 않는다.

## 완료 조건

- 앱 시작 시 백업이 생성되고, 같은 날 재시작해도 중복 생성되지 않는다.
- 11번째 백업 생성 시 가장 오래된 백업이 삭제된다.
- 백업 폴더 열기와 마지막 백업 시각 표시가 동작한다.

## 검증

- 회귀 테스트: 보관 정책, 1일 1회 규칙, 부분 실패 시 동작.
- 백업 폴더 내용과 수동 복원 절차를 실제로 수행해 확인한다.

### 실행 결과

- `dotnet build HmDesktopCalendar.csproj -c Debug`: 성공(경고 0, 오류 0).
- `dotnet run --project tests/HmDesktopCalendar.RegressionTests/HmDesktopCalendar.RegressionTests.csproj -c Debug`:
  66개 회귀 테스트 성공.
- 하루 한 번 제한, 루트·계정 상대 경로와 `settings.json` 제외, 11번째 생성 시
  가장 오래된 세대 삭제, 복사 실패 시 임시 세대 정리를 회귀 테스트로 검증했다.
- 실제 앱 실행으로 `%LocalAppData%\HmDesktopCalendar\backups\20260716-131120`을
  생성하고 루트 `calendar-v2.json`, `todos.json`과 계정별 파일이 포함되며
  `settings.json`은 제외된 것을 확인했다.
- 수동 복원 절차: `docs/backup-restore.md`.
- Windows UI 자동화로 마지막 백업 시각과 **백업 폴더 열기** 버튼의 렌더링·접근성
  요소를 확인했다.
- 스크린샷: `docs/screenshots/190/190-local-backup.png`.

## 작업 결과

- 커밋: 미정
- PR: 미정
